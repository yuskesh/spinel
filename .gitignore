*~
*.swp
*.swo
.DS_Store
*.o

# Build artifacts
/build/
/bin/
/vendor/
/lib/regexp/libspre.a
lib/libspinel_rt.a
lib/libspinel_rt_mt.a
lib/libspinel_rt_mt_tsan.a
lib/libspinel_rt_arena.a
lib/libspbi.a
packages/*/build/
# Dev convenience symlink to bin/spinel (created by `make`)
/spinel

# Temp / profiling output
/tmp/
/perf.data
/perf.data.old

# Local config and machine-local Makefile overrides
.claude/
CLAUDE.local.md
/local.mk
/config.mk

# Analyze-only emit artifacts (--emit-types / --emit-symbol-map, and the
# symbol map --profile writes beside the binary).
# Not *.rbs: test/rbs/ and test/rbs-seed/ carry tracked .rbs fixtures.
*.types.json
*.symbols.json

# Downloaded gems
*.gem
